FROM node:18-alpine

WORKDIR /app

# 复制 API 源码
COPY e:/netease-api/api-enhanced-main/package*.json ./
COPY e:/netease-api/api-enhanced-main/ ./

# 安装依赖
RUN npm install --production

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "app.js"]
